#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../composer-documentation/bin/opus" "$@"
  ret=$?
else 
  node  "$basedir/../composer-documentation/bin/opus" "$@"
  ret=$?
fi
exit $ret
